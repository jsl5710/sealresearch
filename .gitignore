node_modules
dist
.DS_Store
.env
.env.local
*.log

# Budget data now lives in the private repo jsl5710/seal-budget.
# Never reintroduce it here: anything under src/ is compiled into the
# public JS bundle.
src/data/budget.json
